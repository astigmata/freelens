[project]
name = "freelens"
version = "0.1.0"
description = "A lightweight, web-based Kubernetes cluster explorer built with Dash."
requires-python = ">=3.10"
dependencies = [
    "dash>=3.0.0",
    "kubernetes>=32.0.0",
]

[tool.setuptools.packages.find]
include = ["freelens*"]
